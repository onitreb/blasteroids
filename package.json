{
  "name": "blasteroids",
  "private": true,
  "type": "module",
  "description": "Asteroids-ish arcade game (no-server, file:// friendly build).",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:watch": "node scripts/build.mjs --watch",
    "clean": "node scripts/build.mjs --clean",
    "lan:server": "node server/lan-server.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "playwright": "^1.58.2"
  },
  "dependencies": {
    "@colyseus/core": "0.17.32",
    "@colyseus/schema": "4.0.8",
    "@colyseus/sdk": "0.17.26",
    "@colyseus/ws-transport": "0.17.9",
    "colyseus": "0.17.8",
    "express": "5.2.1"
  }
}
